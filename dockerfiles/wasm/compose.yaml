services:
  build:
    build:
      context: ../..
      dockerfile: ./dockerfiles/wasm/Dockerfile
    volumes:
      - type: bind
        source: ../../target
        target: /app/target
      - type: bind
        source: ../../crates
        target: /app/crates
      - type: bind
        source: ../../npm
        target: /app/npm
    working_dir: /app
    command: ["wasm-pack", "build", "npm"]
